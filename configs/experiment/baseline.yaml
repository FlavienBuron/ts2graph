defaults:
  - dataset: airq_small
  - model: stgi
  - training: default
  - graph: ed_rbf_topk
  - _self_

# Top-level Hydra multirun config
hydra:
  mode: MULTIRUN
  sweeper:
    class: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    params:
      graph: empty,fc
