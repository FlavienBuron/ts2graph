# @package _global_

defaults:
  - override /graph: geo_rbf_thresh
  - override /graph/distance: sliding_euclidean
  - _self_

graph:
  name: ${cond:${graph.sparsifier.binary}, 'sed_rbf_thresh_bin', 'sed_rbf_thresh'}
  sparsifier:
    binary: true
    param:
      value: 0.0

hydra:
  sweeper:
    params:
      graph.sparsifier.param.value: range(0.0, 1.01, 0.05)
